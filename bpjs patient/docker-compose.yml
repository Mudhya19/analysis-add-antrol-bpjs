version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DB_HOST=mysql
      - DB_USER=rsds_db
      - DB_PASS=rsdsD4t4b4s3
      - DB_NAME=rsds_db
      - DB_PORT=3306
    depends_on:
      - mysql
    volumes:
      - ./backend:/app
      - ./backend/logs:/app/logs
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  frontend:
#!/bin/bash

# BPJS Patient Registration Analysis System
# Sistem Analisis Pendaftaran Pasien BPJS dengan NLP dan ML

echo "🏥 Setting up BPJS Patient Registration Analysis System..."

# Create main project directory
PROJECT_NAME="bpjs-patient-analysis"
mkdir -p bpjs-patient-analysis
cd bpjs-patient-analysis

# Create backend structure
echo "📁 Creating backend structure..."
mkdir -p backend/{app,models,services,utils,config,data,logs,tests}
mkdir -p backend/app/{api,core,ml,nlp}
mkdir -p backend/app/api/{endpoints,middleware}
mkdir -p backend/models/{database,ml_models,schemas}
mkdir -p backend/services/{bpjs,analytics,reporting}
mkdir -p backend/utils/{database,preprocessing,validation}

# Create frontend structure  
echo "🎨 Creating frontend structure..."
mkdir -p frontend/{src,public,tests}
mkdir -p frontend/src/{components,pages,services,utils,hooks,contexts,assets}
mkdir -p frontend/src/components/{common,charts,forms,tables}
mkdir -p frontend/src/pages/{dashboard,analytics,reports,settings}
mkdir -p frontend/src/services/{api,auth}
mkdir -p frontend/src/assets/{images,styles}

# Create documentation and config
mkdir -p {docs,config,scripts,docker}

# Create main files
touch README.md
touch docker-compose.yml
touch .gitignore
touch .env.example

# Backend files
cat > backend/requirements.txt << EOF
# Core Dependencies
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# Database
mysqlclient==2.2.0
SQLAlchemy==2.0.23
pandas==2.1.3

# Machine Learning & NLP
scikit-learn==1.3.2
nltk==3.8.1
textblob==0.17.1
wordcloud==1.9.2
matplotlib==3.8.2
seaborn==0.13.0
plotly==5.17.0

# Data Processing
numpy==1.25.2
scipy==1.11.4

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-decouple==3.8

# Development
pytest==7.4.3
black==23.11.0
flake8==6.1.0
isort==5.12.0
